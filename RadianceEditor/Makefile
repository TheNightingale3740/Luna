# GNU Make project makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

.PHONY: clean prebuild

SHELLTYPE := posix
ifeq ($(shell echo "test"), "test")
	SHELLTYPE := msdos
endif

# Configurations
# #############################################

ifeq ($(origin CC), default)
  CC = clang
endif
ifeq ($(origin CXX), default)
  CXX = clang++
endif
ifeq ($(origin AR), default)
  AR = ar
endif
RESCOMP = windres
INCLUDES += -I../Radiance/Source -I../Radiance/Vendor/glfw/include -I../Radiance/Vendor/glm -I../Radiance/Vendor/imgui -I../Radiance/Vendor/metal-cpp -I../Radiance/Vendor/spdlog/include
FORCE_INCLUDE +=
ALL_CPPFLAGS += $(CPPFLAGS) -MD -MP $(DEFINES) $(INCLUDES)
ALL_RESFLAGS += $(RESFLAGS) $(DEFINES) $(INCLUDES)
ALL_LDFLAGS += $(LDFLAGS) -L../Radiance/Vendor/glfw/lib -arch arm64
LINKCMD = $(CXX) -o "$@" $(OBJECTS) $(RESOURCES) $(ALL_LDFLAGS) $(LIBS)
define PREBUILDCMDS
endef
define PRELINKCMDS
endef
define POSTBUILDCMDS
endef

ifeq ($(config),debug)
TARGETDIR = ../bin/Debug/RadianceEditor.app/Contents/MacOS
TARGET = $(TARGETDIR)/RadianceEditor
OBJDIR = ../bin-int/Debug/RadianceEditor
DEFINES += -DRD_DEBUG
ALL_CFLAGS += $(CFLAGS) $(ALL_CPPFLAGS) -arch arm64 -g
ALL_CXXFLAGS += $(CXXFLAGS) $(ALL_CPPFLAGS) -arch arm64 -g -std=c++23
LIBS += ../bin/Debug/RadianceEditor.app/Contents/MacOS/libRadiance.a -framework Metal -framework MetalKit -framework Cocoa -framework QuartzCore -framework Foundation -framework IOKit -framework CoreVideo -lglfw3
LDDEPS += ../bin/Debug/RadianceEditor.app/Contents/MacOS/libRadiance.a

else ifeq ($(config),release)
TARGETDIR = ../bin/Release/RadianceEditor.app/Contents/MacOS
TARGET = $(TARGETDIR)/RadianceEditor
OBJDIR = ../bin-int/Release/RadianceEditor
DEFINES += -DRD_RELEASE
ALL_CFLAGS += $(CFLAGS) $(ALL_CPPFLAGS) -arch arm64 -O2
ALL_CXXFLAGS += $(CXXFLAGS) $(ALL_CPPFLAGS) -arch arm64 -O2 -std=c++23
LIBS += ../bin/Release/RadianceEditor.app/Contents/MacOS/libRadiance.a -framework Metal -framework MetalKit -framework Cocoa -framework QuartzCore -framework Foundation -framework IOKit -framework CoreVideo -lglfw3
LDDEPS += ../bin/Release/RadianceEditor.app/Contents/MacOS/libRadiance.a

else ifeq ($(config),dist)
TARGETDIR = ../bin/Dist/RadianceEditor.app/Contents/MacOS
TARGET = $(TARGETDIR)/RadianceEditor
OBJDIR = ../bin-int/Dist/RadianceEditor
DEFINES += -DRD_DIST
ALL_CFLAGS += $(CFLAGS) $(ALL_CPPFLAGS) -arch arm64 -O2
ALL_CXXFLAGS += $(CXXFLAGS) $(ALL_CPPFLAGS) -arch arm64 -O2 -std=c++23
LIBS += ../bin/Dist/RadianceEditor.app/Contents/MacOS/libRadiance.a -framework Metal -framework MetalKit -framework Cocoa -framework QuartzCore -framework Foundation -framework IOKit -framework CoreVideo -lglfw3
LDDEPS += ../bin/Dist/RadianceEditor.app/Contents/MacOS/libRadiance.a

endif

# Per File Configurations
# #############################################


# File sets
# #############################################

GENERATED :=
OBJECTS :=

GENERATED += $(OBJDIR)/EditorMain.o
OBJECTS += $(OBJDIR)/EditorMain.o

# Rules
# #############################################

all: $(TARGET)
	@:

$(TARGET): $(GENERATED) $(OBJECTS) $(LDDEPS) | $(TARGETDIR)
	$(PRELINKCMDS)
	@echo Linking RadianceEditor
	$(SILENT) $(LINKCMD)
	$(POSTBUILDCMDS)

$(TARGETDIR):
	@echo Creating $(TARGETDIR)
ifeq (posix,$(SHELLTYPE))
	$(SILENT) mkdir -p $(TARGETDIR)
else
	$(SILENT) mkdir $(subst /,\\,$(TARGETDIR))
endif

$(OBJDIR):
	@echo Creating $(OBJDIR)
ifeq (posix,$(SHELLTYPE))
	$(SILENT) mkdir -p $(OBJDIR)
else
	$(SILENT) mkdir $(subst /,\\,$(OBJDIR))
endif

clean:
	@echo Cleaning RadianceEditor
ifeq (posix,$(SHELLTYPE))
	$(SILENT) rm -f  $(TARGET)
	$(SILENT) rm -rf $(GENERATED)
	$(SILENT) rm -rf $(OBJDIR)
else
	$(SILENT) if exist $(subst /,\\,$(TARGET)) del $(subst /,\\,$(TARGET))
	$(SILENT) $(foreach f,$(subst /,\\,$(GENERATED)),if exist $(f) del /s /q $(f) >nul &)
	$(SILENT) if exist $(subst /,\\,$(OBJDIR)) rmdir /s /q $(subst /,\\,$(OBJDIR))
endif

prebuild: | $(OBJDIR)
	$(PREBUILDCMDS)

ifneq (,$(PCH))
$(OBJECTS): $(GCH) | $(PCH_PLACEHOLDER)
$(GCH): $(PCH) | prebuild
	@echo $(notdir $<)
	$(SILENT) $(CXX) -x c++-header $(ALL_CXXFLAGS) -o "$@" -MF "$(@:%.gch=%.d)" -c "$<"
$(PCH_PLACEHOLDER): $(GCH) | $(OBJDIR)
ifeq (posix,$(SHELLTYPE))
	$(SILENT) touch "$@"
else
	$(SILENT) echo $null >> "$@"
endif
else
$(OBJECTS): | prebuild
endif


# File Rules
# #############################################

$(OBJDIR)/EditorMain.o: Source/EditorMain.cpp
	@echo "$(notdir $<)"
	$(SILENT) $(CXX) $(ALL_CXXFLAGS) $(FORCE_INCLUDE) -o "$@" -MF "$(@:%.o=%.d)" -c "$<"

-include $(OBJECTS:%.o=%.d)
ifneq (,$(PCH))
  -include $(PCH_PLACEHOLDER).d
endif